<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
     mapper는
     1. dao 인터페이스와 동일한 패키지에 위치해야한다.
     2. dao 인터페이스의 풀네임은 namespace로 지정해야한다.
     3. mapper 인터페이스와 xml의 이름이 동일해야한다.
     4. 작성한 메소드의 이름과 id가 일치하고, 리턴 타입이 일치하는 쿼리문을 모두 작성해야한다.
-->
<mapper namespace="com.bobjo.mini.model.dao.MenuMapper">

    <!-- 조회한 컬럼과 DTO를 매핑시키기위한설정
         config 파일에서 alias설정을 추가하면 type설정에 full class명을 쓰지 않아도 된다.
    -->
    <resultMap id="menuResultMap" type="com.bobjo.mini.model.dto.MenuDTO">
        <id property="menuNum" column="menuNum"/>
        <result property="menuName" column="menuName"/>
        <result property="categoryNum" column="categoryNum"/>
        <result property="countingChoices" column="countingChoices"/>
    </resultMap>

    <!--
        resultType : 이미 정의 되어 있는 타입을 이용하여 리턴 타입으로 이용하는 경우
        resultMap : 위에서 생성한 resultMap을 리턴 타입으로 이용하는 경우
    -->
    <select id="selectAllMenu" resultMap="menuResultMap">
        SELECT
                menuNum,
                menuName,
                categoryNum,
                countingChoices
        FROM    tbl_menu
        ORDER BY menuNum
    </select>

<!--    &lt;!&ndash;-->
<!--       파라미터가 한 개인 경우 바인딩 되는 이름은 상관없으며, 파라미터 타입을-->
<!--       지정하지 않아도 된다.-->
<!--    &ndash;&gt;-->
<!--    <select id="selectMenuByCode" parameterType="_int" resultMap="menuResultMap">-->
<!--        SELECT-->
<!--                menu_code,-->
<!--                menu_name,-->
<!--                menu_price,-->
<!--                category_code,-->
<!--                orderable_status-->
<!--        FROM    tbl_menu-->
<!--        WHERE   orderable_status = 'Y'-->
<!--          AND   menu_code = #{ code }-->
<!--    </select>-->
<!--    &lt;!&ndash;-->
<!--        insert, update, delete의 경우 resultType은 작성하지 않고, 기본 _int로 수행 결과를 반환한다.-->
<!--        파라미터로 전달된 DTO의 필드명으로 값을 바인딩 해야 한다. 파라미터 타입도 생략이 가능하다.-->
<!--    &ndash;&gt;-->
    <insert id="insertMenu" parameterType="com.bobjo.mini.model.dto.MenuDTO">
        INSERT
          INTO tbl_menu (
                            menuName,
                            categoryNum
                        )
        VALUES (
                 #{ menuName },
                 #{ categoryNum }
               )
    </insert>

<!--    <update id="updateMenu">-->
<!--        UPDATE tbl_menu-->
<!--        SET    menu_name = #{ name },-->
<!--               menu_price = #{ price },-->
<!--               category_code = #{ categoryCode }-->
<!--        WHERE  menu_code = #{ code }-->
<!--    </update>-->

<!--    <delete id="deleteMenu" parameterType="_int">-->
<!--        DELETE-->
<!--          FROM tbl_menu-->
<!--        WHERE  menu_code = #{ code }-->
<!--    </delete>-->
</mapper>